[package]
name = "idle_tone_checker"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustfft = "6.4.1"
veryl-component = "0.1.0"

# Standalone package: keep it out of any enclosing cargo workspace.
[workspace]
